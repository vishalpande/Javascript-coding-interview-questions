// Given two numbers a and b. Return the remainder when a is divided by b.

// Example 1:
// Input:
// 5 8

// Output:
// 5

// Explanation : -

function findRemainder(a, b) {
    return a%b;
}
    var a = 5;
    var b =8;
    console.log(findRemainder(a, b));